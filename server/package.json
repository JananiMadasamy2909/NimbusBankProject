{
  "name": "nimbus-bank-server",
  "version": "1.0.0",
  "description": "Backend API + static host for the Nimbus Bank QA sandbox",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "reset": "node -e \"require('./db').reset(); console.log('Data reset.')\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}